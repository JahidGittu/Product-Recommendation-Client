import {
    createBrowserRouter,
    RouterProvider,
} from "react-router";

const router = createBrowserRouter([
    {
        path: "/",
        element: <h1>alskdjf;lkasdj</h1>,
    },

]);

export default router;